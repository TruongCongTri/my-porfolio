// Use for Reusable Layouts in components/layouts

import * as React from 'react';
import { cn } from '@/lib/utils';

export interface {{pascalCase name}}Props extends React.HTMLAttributes<HTMLDivElement> {
  children: React.ReactNode;
}

export function {{pascalCase name}}({ children, className, ...props }: {{pascalCase name}}Props) {
  return (
    <div 
      className={cn(
        "w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8", // Default classes for Layout
        className
      )} 
      {...props}
    >
      {children}
    </div>
  );
}